name: CI

on:
  pull_request:
    branches: [main]  # CI runs on all PRs targeting main
    types: [opened, synchronize, reopened]
  push:
    branches:
      - develop  # Optional: run CI on develop branch pushes
      - 'release-please--**'  # Also run CI on release-please branches

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      # Quick pass for release-please PRs (already tested before merge to main)
      - name: Check if release PR
        id: check-release
        run: |
          if [[ "${{ github.head_ref }}" == release-please--* ]]; then
            echo "is_release=true" >> $GITHUB_OUTPUT
            echo "✅ Release PR detected - skipping tests (already tested in main)"
          else
            echo "is_release=false" >> $GITHUB_OUTPUT
          fi

      - uses: actions/checkout@v5
        if: steps.check-release.outputs.is_release != 'true'

      - uses: pnpm/action-setup@v4
        if: steps.check-release.outputs.is_release != 'true'

      - name: Use Node.js ${{ matrix.node-version }}
        if: steps.check-release.outputs.is_release != 'true'
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        if: steps.check-release.outputs.is_release != 'true'
        run: pnpm install --frozen-lockfile

      - name: Run linter
        if: steps.check-release.outputs.is_release != 'true'
        run: pnpm lint

      - name: Run type check
        if: steps.check-release.outputs.is_release != 'true'
        run: pnpm tsc --noEmit

      - name: Check package.json description length
        if: steps.check-release.outputs.is_release != 'true'
        run: |
          DESCRIPTION=$(node -p "require('./package.json').description")
          LENGTH=${#DESCRIPTION}
          echo "Description: $DESCRIPTION"
          echo "Length: $LENGTH characters"
          if [ $LENGTH -gt 132 ]; then
            echo "❌ Error: Description exceeds 132 characters (current: $LENGTH)"
            echo "This is required for Chrome Web Store submission."
            exit 1
          else
            echo "✅ Description length is valid ($LENGTH/132 characters)"
          fi

      - name: Run tests
        if: steps.check-release.outputs.is_release != 'true'
        run: pnpm test run

      - name: Build extension (dry run)
        if: steps.check-release.outputs.is_release != 'true'
        run: pnpm build

      - name: Verify build output
        if: steps.check-release.outputs.is_release != 'true'
        run: |
          if [ ! -d "dist" ]; then
            echo "Build failed: dist directory not found"
            exit 1
          fi
          if [ ! -f "dist/manifest.json" ]; then
            echo "Build failed: manifest.json not found"
            exit 1
          fi
          echo "Build verification passed ✓"

      - name: Upload build artifacts (for PR review)
        if: github.event_name == 'pull_request' && steps.check-release.outputs.is_release != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: pr-build-${{ github.event.pull_request.number }}
          path: dist/
          retention-days: 7